# potentially for character selection screens
query getUserAavegotchis($id: ID!) {
  user(id: $id) {
    gotchisOwned {
      id
      name
      level
      baseRarityScore
      modifiedRarityScore
      numericTraits
    }
  }
}
